import { useState } from "react"
import { GetProfile } from "../../utils/ProfileFetch"
import { Spinner } from "react-bootstrap"


export const ProfileCompo = () => {
    const [getSpinner , setgetSpinner] = useState(false)
    return(
        <>
        <h1>tester</h1>
        </>
    )
}